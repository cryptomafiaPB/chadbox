services:
  chadbox-api:
    build: .
    container_name: chadbox_core
    privileged: true
    volumes:
      - .:/app
      # - /sys/fs/cgroup:/sys/fs/cgroup:rw  # Maps the cgroup filesystem securely        
    working_dir: /app